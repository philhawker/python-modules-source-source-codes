# Set requires all the data elements to be unique

tags = {
  'python',
  'coding',
  'tutorials',
  'coding'
}

print(tags)

query_one = 'python' in tags   # will return True
query_two = 'ruby' in tags   #will return False


print(query_one)
print(query_two)